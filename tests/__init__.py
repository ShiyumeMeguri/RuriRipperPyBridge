"""Host-free self-tests. Run them with ``python run_tests.py`` from the repo
root, or through any unittest discovery whose top-level dir is the package's
parent (the tests import the package relatively, so its checkout folder can be
named anything -- which it is, since each host names the submodule itself)."""
